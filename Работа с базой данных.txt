1)

SELECT c.login, COUNT* (o.id) AS deliveri_order
FROM "Couriers" AS c
JOIN "Orders" AS O ON c.id = o."courierId"
WHERE o."inDelivery" = true
GROUP BY c."login";



2) 

SELECT cancelled,
finished,
inDelivery 
          CASE
          WHEN finished = true THEN 2,
          WHEN cancelled = true THEN -1,
          WHEN inDelivery = ture THEN 1,
          ELSE 0
       END AS status
FROM "Orders";





